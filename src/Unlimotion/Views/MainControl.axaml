<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:Unlimotion.ViewModel;assembly=Unlimotion.ViewModel"
             xmlns:unlimotion="clr-namespace:Unlimotion"
             xmlns:views="clr-namespace:Unlimotion.Views"
             xmlns:converters="clr-namespace:Unlimotion.Converters"
             xmlns:behavior="clr-namespace:Unlimotion.Behavior"
             xmlns:domain="clr-namespace:Unlimotion.Domain;assembly=Unlimotion.Domain"
             xmlns:searchControl="clr-namespace:Unlimotion.Views.SearchControl"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:DataType="viewModel:MainWindowViewModel"
             Name="mainControl"
             x:Class="Unlimotion.Views.MainControl">

    <Design.DataContext>
        <viewModel:MainWindowViewModel/>
    </Design.DataContext>
    <UserControl.Resources>
        <SolidColorBrush x:Key="CurrentTaskDetailsPanelBackgroundBrush" Color="{DynamicResource SurfaceRailColor}"/>
        <SolidColorBrush x:Key="CurrentTaskDetailsPanelBorderBrush" Color="{DynamicResource BorderStrongColor}"/>
        <unlimotion:TimeSpanStringConverter x:Key="TimeSpanStringConverter"/>
        <unlimotion:EqualConverter x:Key="EqualConverter"/>
        <unlimotion:NotEqualConverter x:Key="NotEqualConverter"/>
        <DataTemplate x:Key="TaskStatusOptionDropDownTemplate" DataType="viewModel:TaskStatusOption">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8" MinHeight="24">
                <unlimotion:TaskStatusIcon Status="{Binding Status}"
                                           Width="20"
                                           Height="20"
                                           VerticalAlignment="Center"
                                           IsEnabled="{Binding IsEnabled}"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding Title}"
                           VerticalAlignment="Center"
                           IsEnabled="{Binding IsEnabled}"
                           ToolTip.Tip="{Binding ToolTip}"/>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <UserControl.Styles>
        <Style Selector="Button.TaskStatusPicker">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="MinWidth" Value="20"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="Button.TaskStatusPicker:pointerover">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style Selector="TreeView.TaskTree Label">
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style Selector="TreeView.TaskTree Label.TaskTreeMetaLabel">
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>
        <Style Selector="Expander.TaskStatusHistoryExpander">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <Grid MinWidth="{TemplateBinding MinWidth}"
                          MaxWidth="{TemplateBinding MaxWidth}"
                          RowDefinitions="Auto,Auto">
                        <ToggleButton x:Name="PART_HeaderSite"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Left"
                                      Content="{TemplateBinding Header}"
                                      CornerRadius="{TemplateBinding CornerRadius}"
                                      IsEnabled="{TemplateBinding IsEnabled}"
                                      IsChecked="{TemplateBinding IsExpanded, Mode=TwoWay}"/>
                        <Border Grid.Row="1"
                                x:Name="ExpanderContent"
                                IsVisible="{TemplateBinding IsExpanded}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,0,1,1"
                                MinHeight="{TemplateBinding MinHeight}"
                                HorizontalAlignment="Stretch"
                                Padding="8">
                            <ContentPresenter x:Name="PART_ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter>
        </Style>
        <Style Selector="Grid.FilterToolbar">
            <Setter Property="ColumnSpacing" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <!-- Clickable breadcrumb crumbs + separators -->
        <Style Selector="unlimotion|EmojiTextBlock.breadcrumb-crumb">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="{DynamicResource FsBody}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text2Brush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MaxWidth" Value="220"/>
        </Style>
        <Style Selector="unlimotion|EmojiTextBlock.breadcrumb-crumb:pointerover">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style Selector="TextBlock.breadcrumb-sep">
            <Setter Property="FontSize" Value="{DynamicResource FsBody}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text3Brush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="6,0"/>
        </Style>
        <!-- Slim, docked, coloured sidebar scrollbar: thin but clearly a scrollbar, and it
             reserves its own gutter so it never overlaps the nav items. -->
        <Style Selector="ScrollViewer.sidebar-scroll ScrollBar:vertical">
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
        </Style>
        <Style Selector="ScrollViewer.sidebar-scroll ScrollBar:vertical Thumb">
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="CornerRadius" Value="{DynamicResource RadiusPill}"/>
        </Style>
        <Style Selector="ScrollViewer.sidebar-scroll ScrollBar:vertical Thumb:pointerover">
            <Setter Property="Background" Value="{DynamicResource AccentBrightBrush}"/>
        </Style>
        <Style Selector="WrapPanel.FilterToolbarItems">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style Selector="searchControl|SearchBar.FilterToolbarSearch">
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
        <Style Selector="Border.FilterOverflowPanel">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlMidBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
        </Style>
        <Style Selector="ScrollViewer.FilterPanelScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <Style Selector="TextBlock.FilterPanelGroupTitle">
            <Setter Property="FontSize" Value="{DynamicResource AppSmallFontSize}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Opacity" Value="0.72"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style Selector="Button.FilterPanelResetButton">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="DropDownButton.FilterToolbarFiltersButton">
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="{DynamicResource AppSearchControlHeight}"/>
            <Setter Property="MinWidth" Value="44"/>
            <Setter Property="MinHeight" Value="{DynamicResource AppSearchControlHeight}"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="PathIcon.FilterToolbarFiltersIcon">
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Foreground" Value="{DynamicResource Text2Brush}"/>
        </Style>
        <!-- Aurora: drop the dropdown chevron so it reads as a clean filter-icon button -->
        <Style Selector="DropDownButton.FilterToolbarFiltersButton /template/ PathIcon">
            <Setter Property="IsVisible" Value="False"/>
        </Style>
        <Style Selector="PathIcon.FilterPanelResetIcon">
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel">
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Spacing" Value="8"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact">
            <Setter Property="Margin" Value="6"/>
            <Setter Property="Spacing" Value="6"/>
        </Style>
        <Style Selector="Border.CurrentTaskDetailsPanelFrame">
            <Setter Property="Background" Value="{DynamicResource CurrentTaskDetailsPanelBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CurrentTaskDetailsPanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource RadiusCard}"/>
        </Style>
        <Style Selector="WrapPanel.TaskCommandBar">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,4"/>
        </Style>
        <Style Selector="Button.TaskCommandButton">
            <Setter Property="Margin" Value="0,0,6,4"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
        <Style Selector="Button.TaskCommandPrimaryButton">
            <Setter Property="Background" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style Selector="Button.TaskCommandSecondaryButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlMidBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style Selector="Button.TaskCommandDangerButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BlockedBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BlockedLineBrush}"/>
        </Style>
        <Style Selector="DropDownButton.TaskCommandOverflowButton">
            <Setter Property="IsVisible" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeControlMidBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,6,4"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
        <Style Selector="DropDownButton.TaskAccentOutlineButton">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeControlLowBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
        <Style Selector="Button.TaskAccentOutlineButton">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeControlLowBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
        <Style Selector="DropDownButton.TaskIconOnlyDropDownButton">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter>
        </Style>
        <Style Selector="DropDownButton.TaskAccentOutlineButton:pointerover">
            <Setter Property="Background" Value="{DynamicResource ThemeControlMidBrush}"/>
        </Style>
        <Style Selector="Button.TaskAccentOutlineButton:pointerover">
            <Setter Property="Background" Value="{DynamicResource ThemeControlMidBrush}"/>
        </Style>
        <Style Selector="DropDownButton.TaskActionsMenuButton">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinWidth" Value="36"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontSize" Value="17"/>
        </Style>
        <Style Selector="DropDownButton.TaskCreateMenuButton">
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="MinWidth" Value="42"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>
        <Style Selector="MenuItem.TaskCreateMenuItem">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="Padding" Value="12,10"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact Button.TaskCommandSecondaryButton">
            <Setter Property="IsVisible" Value="False"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact DropDownButton.TaskCommandOverflowButton">
            <Setter Property="IsVisible" Value="True"/>
        </Style>
        <Style Selector="Button.MainTabsOverflowButton">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="{DynamicResource AppTabMinHeight}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style Selector="Button.MainTabsOverflowButton:pointerover">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
        <Style Selector="Button.MainTabsOverflowButton:pressed">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
        <Style Selector="Border.CurrentTaskCard">
            <Setter Property="Padding" Value="10"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact Border.CurrentTaskCard">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="CornerRadius" Value="6"/>
        </Style>
        <Style Selector="StackPanel.CurrentTaskCardContent">
            <Setter Property="Spacing" Value="8"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact StackPanel.CurrentTaskCardContent">
            <Setter Property="Spacing" Value="6"/>
        </Style>
        <Style Selector="Grid.TaskCardHeader">
            <Setter Property="ColumnSpacing" Value="6"/>
            <Setter Property="RowSpacing" Value="6"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact Grid.TaskCardHeader">
            <Setter Property="ColumnSpacing" Value="6"/>
            <Setter Property="RowSpacing" Value="6"/>
        </Style>
        <Style Selector="CheckBox.TaskHeaderCompletionCheckBox">
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
        <!-- tags + Wanted + importance row -->
        <Style Selector="WrapPanel.TaskHeaderTagsRow">
            <Setter Property="Orientation" Value="Horizontal"/>
        </Style>
        <Style Selector="WrapPanel.TaskHeaderTagsRow > Border">
            <Setter Property="Margin" Value="0,0,6,4"/>
        </Style>
        <Style Selector="WrapPanel.TaskHeaderTagsRow > StackPanel">
            <Setter Property="Margin" Value="2,0,0,4"/>
        </Style>
        <Style Selector="CheckBox.TaskHeaderStateCheckBox">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Margin" Value="0,0,12,4"/>
        </Style>
        <Style Selector="NumericUpDown.TaskHeaderImportanceInput">
            <Setter Property="Width" Value="84"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
        <!-- compact metadata line (id + timestamps) -->
        <Style Selector="WrapPanel.TaskHeaderMetaLine">
            <Setter Property="Orientation" Value="Horizontal"/>
        </Style>
        <Style Selector="WrapPanel.TaskHeaderMetaLine > StackPanel">
            <Setter Property="Margin" Value="0,0,14,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style Selector="TextBlock.TaskHeaderMetaLabel">
            <Setter Property="Foreground" Value="{DynamicResource Text2Brush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style Selector="TextBlock.TaskHeaderIdMeta">
            <Setter Property="MaxWidth" Value="140"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
        <Style Selector="TextBox.CurrentTaskTitleEditor">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="TextBox.BorderlessTextBoxChrome">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.BorderlessTextBoxChrome /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.BorderlessTextBoxChrome:focus /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.BorderlessTextBoxChrome:pointerover /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.BorderlessTextBoxChrome:focus:pointerover /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact TextBox.CurrentTaskTitleEditor">
            <Setter Property="MinHeight" Value="34"/>
        </Style>
        <Style Selector="Border.TaskCardSection">
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
            <Setter Property="Padding" Value="0,10,0,0"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact Border.TaskCardSection">
            <Setter Property="Padding" Value="0,5,0,0"/>
        </Style>
        <Style Selector="StackPanel.TaskCardSectionContent">
            <Setter Property="Spacing" Value="5"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact StackPanel.TaskCardSectionContent">
            <Setter Property="Spacing" Value="4"/>
        </Style>
        <Style Selector="TextBlock.TaskCardSectionTitle">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="{DynamicResource FsEyebrow}"/>
            <Setter Property="LetterSpacing" Value="{DynamicResource LsEyebrow}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text3Brush}"/>
        </Style>
        <Style Selector="TextBox.TaskDescriptionEditor">
            <Setter Property="MinHeight" Value="82"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact TextBox.TaskDescriptionEditor">
            <Setter Property="MinHeight" Value="72"/>
        </Style>
        <Style Selector="Grid.TaskPlanningFieldRow">
            <Setter Property="ColumnSpacing" Value="4"/>
            <Setter Property="Margin" Value="0,0,8,6"/>
        </Style>
        <Style Selector="CalendarDatePicker.TaskPlanningValueControl">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Padding" Value="10,6,6,5"/>
        </Style>
        <Style Selector="TextBox.TaskPlanningValueControl">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinHeight" Value="40"/>
        </Style>
        <Style Selector="DropDownButton.TaskPlanningQuickAction">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="40"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text2Brush}"/>
            <Setter Property="CornerRadius" Value="{DynamicResource RadiusMd}"/>
        </Style>
        <Style Selector="DropDownButton.TaskPlanningQuickAction:pointerover">
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
        </Style>
        <Style Selector="Grid.RepeaterControls">
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>
        <Style Selector="Grid.RepeaterPatternControls">
            <Setter Property="ColumnSpacing" Value="0"/>
            <Setter Property="RowSpacing" Value="0"/>
        </Style>
        <Style Selector="ComboBox.RepeaterSelector">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="0,0,6,5"/>
        </Style>
        <Style Selector="ComboBox.RepeaterPatternTypeSelector">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="0,0,6,5"/>
        </Style>
        <Style Selector="NumericUpDown.RepeaterPeriodInput">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="0,0,6,5"/>
        </Style>
        <Style Selector="CheckBox.RepeaterAfterCompleteCheckBox">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Margin" Value="0,0,6,5"/>
        </Style>
        <Style Selector="WrapPanel.WeekdayToggles">
            <Setter Property="Orientation" Value="Horizontal"/>
        </Style>
        <Style Selector="ToggleButton.WeekdayToggle">
            <Setter Property="Width" Value="46"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,0,4,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="Grid.RelationSectionHeader">
            <Setter Property="ColumnSpacing" Value="8"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
            <Setter Property="MinHeight" Value="30"/>
        </Style>
        <Style Selector="Grid.RelationSectionHeader Label">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="{DynamicResource FsEyebrow}"/>
            <Setter Property="LetterSpacing" Value="{DynamicResource LsEyebrow}"/>
            <Setter Property="Foreground" Value="{DynamicResource Text3Brush}"/>
        </Style>
        <Style Selector="Button.RelationAddButton">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="32"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style Selector="StackPanel.RelationPickerRow">
            <Setter Property="Spacing" Value="8"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        <Style Selector="Border.RelationEditorCard">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderStrongBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{DynamicResource RadiusSm}"/>
        </Style>
        <Style Selector="StackPanel.TaskDetailsPanel.TaskDetailsCompact Border.RelationEditorCard">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="CornerRadius" Value="6"/>
        </Style>
        <Style Selector="TextBox.RelationEditorSearch">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
        <Style Selector="ListBox.RelationEditorSuggestions">
            <Setter Property="MaxHeight" Value="240"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor">
            <Setter Property="MinHeight" Value="22"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor:focus">
            <Setter Property="Opacity" Value="1"/>
            <Setter Property="IsHitTestVisible" Value="True"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor:focus /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor:pointerover /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor:focus:pointerover /template/ Border#PART_BorderElement">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style Selector="TextBox.InlineTaskTitleEditor.IsWanted">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style Selector="TextBox.CompletionCriterionTextBox">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="Button.TaskRowRemoveButton">
            <Setter Property="MinWidth" Value="28"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource BlockedBrush}"/>
            <Setter Property="Opacity" Value="0.72"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="Button.CompletionCriterionRemoveButton">
            <Setter Property="MinWidth" Value="28"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource BlockedBrush}"/>
            <Setter Property="Opacity" Value="0.72"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style Selector="PathIcon.RemoveButtonIcon">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource BlockedBrush}"/>
        </Style>
        <Style Selector="TreeView.TaskTree">
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu>
                        <MenuItem Header="{DynamicResource ExpandCurrentNested}"
                                  Tag="ExpandCurrentNested"
                                  InputGesture="Ctrl+Shift+Right"
                                  Click="TaskTreeContextMenuItem_OnClick"
                                  AutomationProperties.AutomationId="ExpandCurrentNestedTreeMenuItem"/>
                        <MenuItem Header="{DynamicResource CollapseCurrentNested}"
                                  Tag="CollapseCurrentNested"
                                  InputGesture="Ctrl+Shift+Left"
                                  Click="TaskTreeContextMenuItem_OnClick"
                                  AutomationProperties.AutomationId="CollapseCurrentNestedTreeMenuItem"/>
                        <MenuItem Header="{DynamicResource ExpandAllNodes}"
                                  Tag="ExpandAll"
                                  InputGesture="Ctrl+Alt+Right"
                                  Click="TaskTreeContextMenuItem_OnClick"
                                  AutomationProperties.AutomationId="ExpandAllTreeMenuItem"/>
                        <MenuItem Header="{DynamicResource CollapseAllNodes}"
                                  Tag="CollapseAll"
                                  InputGesture="Ctrl+Alt+Left"
                                  Click="TaskTreeContextMenuItem_OnClick"
                                  AutomationProperties.AutomationId="CollapseAllTreeMenuItem"/>
                        <Separator/>
                        <MenuItem Header="{DynamicResource CopyTaskOutline}"
                                  Tag="CopyOutline"
                                  InputGesture="Ctrl+Shift+C"
                                  Click="TaskTreeContextMenuItem_OnClick"
                                  AutomationProperties.AutomationId="CopyTaskOutlineTreeMenuItem"/>
                        <MenuItem Header="{DynamicResource PasteTaskOutline}"
                                  Tag="PasteOutline"
                                  InputGesture="Ctrl+Shift+V"
                                  Click="TaskTreeContextMenuItem_OnClick"
                                  AutomationProperties.AutomationId="PasteTaskOutlineTreeMenuItem"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
        </Style>
        <Style Selector="TreeView.TaskTree TreeViewItem">
            <Setter Property="IsExpanded">
                <Setter.Value>
                    <Binding Path="((viewModel:TaskWrapperViewModel)DataContext)?.IsExpanded"
                             Mode="TwoWay"
                             RelativeSource="{RelativeSource Mode=Self}"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Styles>
    <UserControl.DataTemplates>
        <DataTemplate DataType="viewModel:TaskStatusOption">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource TaskStatusOptionDropDownTemplate}"/>
        </DataTemplate>
        <DataTemplate DataType="viewModel:WantedFilterOption">
            <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
        </DataTemplate>
        <DataTemplate DataType="viewModel:TaskStatusFilter">
            <Grid ColumnDefinitions="Auto,Auto,*" ColumnSpacing="8">
                <CheckBox IsChecked="{Binding ShowTasks}" VerticalAlignment="Center"/>
                <unlimotion:TaskStatusIcon Grid.Column="1"
                                           Status="{Binding Status}"
                                           Width="20"
                                           Height="20"
                                           VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Title}" VerticalAlignment="Center"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="domain:TaskCompletionCriterion">
            <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="4" Margin="0,0,0,2">
                <CheckBox IsChecked="{Binding IsSatisfied}"
                          VerticalAlignment="Center"
                          AutomationProperties.AutomationId="CompletionCriterionSatisfiedCheckBox"/>
                <TextBox Grid.Column="1"
                         Classes="CompletionCriterionTextBox BorderlessTextBoxChrome"
                         Text="{Binding Text}"
                         TextWrapping="Wrap"
                         PlaceholderText="{DynamicResource CompletionCriterionWatermark}"
                         AutomationProperties.AutomationId="CompletionCriterionTextBox"/>
                <Button Grid.Column="2"
                        Classes="CompletionCriterionRemoveButton"
                        Click="CompletionCriterionRemoveButton_OnClick"
                        ToolTip.Tip="{DynamicResource Remove}"
                        AutomationProperties.AutomationId="CompletionCriterionRemoveButton">
                    <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                </Button>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="domain:TaskStatusHistoryEntry">
            <Grid ColumnDefinitions="Auto,Auto,*" ColumnSpacing="8" Margin="0,0,0,4">
                <TextBlock Text="{Binding Status}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding ChangedAt, StringFormat={}{0:yyyy.MM.dd HH:mm}}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="{Binding Author}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="viewModel:TaskItemViewModel">
            <Grid ColumnDefinitions="Auto, Auto, Auto, *" Background="Transparent" DragDrop.AllowDrop="True">
                <!-- Aurora: green "ready now" dot on unblocked, actionable tasks -->
                <Border Grid.Column="0"
                        Classes="ready-dot"
                        Margin="0,0,7,0"
                        VerticalAlignment="Center"
                        IsVisible="{Binding IsCanBeCompleted}"
                        ToolTip.Tip="{DynamicResource Unlocked}"
                        AutomationProperties.AutomationId="TaskReadyDot"/>
                <unlimotion:TaskStatusPicker Grid.Column="1"
                                             Task="{Binding}"
                                             VerticalAlignment="Center"
                                             AutomationProperties.AutomationId="TaskStatusButton"/>
                <TextBlock Grid.Column="2"
                           Text="{Binding RepeaterListMarker}"
                           IsVisible="{Binding IsHaveRepeater}"
                           ToolTip.Tip="{Binding RepeaterListMarkerToolTip}"
                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                           Margin="4,0,4,0"
                           VerticalAlignment="Center" />
                <unlimotion:EmojiTextBlock Grid.Column="3" EmojiText="{Binding Title}" VerticalAlignment="Center" HorizontalAlignment="Left" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding Wanted}" Classes.IsCanBeCompleted="{Binding !IsCanBeCompleted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"  />
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="viewModel:TaskWrapperViewModel">
            <Grid ColumnDefinitions="*,Auto" Background="Transparent">
                <ContentControl Content="{Binding TaskItem}" MinWidth="0"/>
                <Button Grid.Column="1"
                        Classes="TaskRowRemoveButton"
                        ToolTip.Tip="{DynamicResource Remove}"
                        Command="{Binding RemoveCommand}">
                    <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                </Button>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="viewModel:TaskRelationCandidateViewModel">
            <StackPanel Margin="4,2">
                <TextBlock Text="{Binding Title}" />
                <TextBlock Text="{Binding Context}" Opacity="0.7" FontSize="11" TextWrapping="Wrap"/>
            </StackPanel>
        </DataTemplate>
        <DataTemplate DataType="viewModel:EmojiFilter">
            <Grid ColumnDefinitions="Auto, Auto" >
                <CheckBox IsChecked="{Binding ShowTasks}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="viewModel:UnlockedTimeFilter">
            <CheckBox Content="{Binding Title}" IsChecked="{Binding ShowTasks}" VerticalAlignment="Center"/>
        </DataTemplate>
        <DataTemplate DataType="viewModel:DurationFilter">
            <CheckBox Content="{Binding Title}" IsChecked="{Binding ShowTasks}" VerticalAlignment="Center"/>
        </DataTemplate>
    </UserControl.DataTemplates>

    <UserControl.KeyBindings>
        <KeyBinding Gesture="Shift+Delete" Command="{Binding DeleteSelectedTreeItemsCommand}"/>
        <KeyBinding Gesture="Ctrl+Enter" Command="{Binding CreateSibling}"/>
        <KeyBinding Gesture="Shift+Enter" Command="{Binding CreateBlockedSibling}"/>
        <KeyBinding Gesture="Ctrl+Tab" Command="{Binding CreateInner}"/>
        <KeyBinding Gesture="Ctrl+Shift+Right" Command="{Binding ExpandCurrentNestedCommand}"/>
        <KeyBinding Gesture="Ctrl+Shift+Left" Command="{Binding CollapseCurrentNestedCommand}"/>
        <KeyBinding Gesture="Ctrl+Alt+Right" Command="{Binding ExpandAllTreeNodesCommand}"/>
        <KeyBinding Gesture="Ctrl+Alt+Left" Command="{Binding CollapseAllTreeNodesCommand}"/>
        <KeyBinding Gesture="Ctrl+Shift+C" Command="{Binding CopyTaskOutlineTreeCommand}"/>
        <KeyBinding Gesture="Ctrl+Shift+V" Command="{Binding PasteTaskOutlineTreeCommand}"/>
        <KeyBinding Gesture="Ctrl+D" Command="{Binding CompleteCurrentTaskCommand}"/>
    </UserControl.KeyBindings>

    <Grid RowDefinitions="Auto,*" Background="{DynamicResource BgBrush}">
        <!--Адрес / command bar-->
        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="{DynamicResource SurfaceRailBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                MinHeight="{DynamicResource TopbarH}">
        <Grid Margin="16,0" ColumnDefinitions="Auto,*,Auto,Auto,Auto" ColumnSpacing="10">
            <!-- brand mark -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <Border Width="22" Height="22" CornerRadius="{DynamicResource RadiusSm}"
                        Background="{DynamicResource AccentBrush}"
                        BoxShadow="{DynamicResource GlowAccent}"/>
                <TextBlock Text="Unlimotion" Classes="title" VerticalAlignment="Center"/>
            </StackPanel>
            <!-- clickable breadcrumb: each crumb navigates to that ancestor task -->
            <ItemsControl x:Name="BreadcrumbsTextBlock"
                          Grid.Column="1"
                          AutomationProperties.AutomationId="BreadcrumbsTextBlock"
                          ItemsSource="{Binding BreadScrumbItems}"
                          Margin="14,0,0,0"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="viewModel:BreadcrumbSegment">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <unlimotion:EmojiTextBlock Classes="breadcrumb-crumb"
                                                       EmojiText="{Binding Title}"
                                                       TextWrapping="NoWrap"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxLines="1"
                                                       ToolTip.Tip="{Binding Title}"
                                                       PointerPressed="BreadcrumbCrumb_OnPointerPressed"/>
                            <TextBlock Classes="breadcrumb-sep" Text="/" IsVisible="{Binding !IsLast}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <!-- theme toggle -->
            <Button Grid.Column="2"
                    Classes="subtle"
                    Width="34" Padding="0"
                    Click="ThemeToggle_OnClick"
                    ToolTip.Tip="Сменить тему"
                    AutomationProperties.AutomationId="ThemeToggleButton">
                <PathIcon x:Name="ThemeToggleIcon" Width="17" Height="17" Data="{StaticResource IconMoon}"/>
            </Button>
            <DropDownButton Grid.Column="3"
                            Classes="TaskCreateMenuButton TaskAccentOutlineButton TaskIconOnlyDropDownButton"
                            Content="➕"
                            ToolTip.Tip="{DynamicResource CreateTaskMenuTip}"
                            AutomationProperties.Name="{DynamicResource CreateTaskMenuTip}"
                            AutomationProperties.AutomationId="GlobalTaskCreateMenuButton">
                <DropDownButton.Flyout>
                    <MenuFlyout>
                        <MenuItem Classes="TaskCreateMenuItem"
                                  Header="{DynamicResource NewTask}"
                                  Command="{Binding Create}"
                                  AutomationProperties.AutomationId="GlobalTaskCreateTaskMenuItem"/>
                        <MenuItem Classes="TaskCreateMenuItem"
                                  Header="{DynamicResource NewSibling}"
                                  Command="{Binding CreateSibling}"
                                  AutomationProperties.AutomationId="GlobalTaskCreateSiblingMenuItem"/>
                        <MenuItem Classes="TaskCreateMenuItem"
                                  Header="{DynamicResource NewBlockedSibling}"
                                  Command="{Binding CreateBlockedSibling}"
                                  AutomationProperties.AutomationId="GlobalTaskCreateBlockedSiblingMenuItem"/>
                        <MenuItem Classes="TaskCreateMenuItem"
                                  Header="{DynamicResource NewInner}"
                                  Command="{Binding CreateInner}"
                                  AutomationProperties.AutomationId="GlobalTaskCreateInnerMenuItem"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
            <!-- avatar (teal → blue gradient); click opens the personal profile ("личный кабинет") -->
            <Border Grid.Column="4"
                    x:Name="UserAvatar"
                    Width="38" Height="38"
                    CornerRadius="{DynamicResource RadiusPill}"
                    ClipToBounds="True"
                    Cursor="Hand"
                    VerticalAlignment="Center"
                    PointerPressed="UserAvatar_OnPointerPressed"
                    ToolTip.Tip="{Binding Profile.DisplayName}"
                    AutomationProperties.AutomationId="UserAvatar">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,100%">
                        <GradientStop Offset="0" Color="{DynamicResource TealColor}"/>
                        <GradientStop Offset="1" Color="{DynamicResource AccentColor}"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Panel>
                    <TextBlock Text="{Binding Profile.Initials}"
                               IsVisible="{Binding Profile.HasNoAvatar}"
                               Foreground="White"
                               FontWeight="SemiBold"
                               FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    <!-- Same circular-crop framing as the profile editor, scaled to 38px -->
                    <Image Source="{Binding Profile.AvatarAbsolutePath, Converter={converters:ImagePathConverter}}"
                           IsVisible="{Binding Profile.HasAvatar}"
                           Width="38" Height="38"
                           Stretch="UniformToFill"
                           RenderTransformOrigin="50%,50%">
                        <Image.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform ScaleX="{Binding Profile.AvatarZoom}" ScaleY="{Binding Profile.AvatarZoom}"/>
                                <TranslateTransform X="{Binding Profile.AvatarOffsetX, Converter={converters:ScaleByConverter}, ConverterParameter=38}"
                                                    Y="{Binding Profile.AvatarOffsetY, Converter={converters:ScaleByConverter}, ConverterParameter=38}"/>
                            </TransformGroup>
                        </Image.RenderTransform>
                    </Image>
                </Panel>
            </Border>
        </Grid>
        </Border>

        <!-- Aurora: left sidebar (VIEWS) + content -->
        <Grid Grid.Row="1" ColumnDefinitions="Auto,*">

            <!-- LEFT SIDEBAR (collapses on narrow widths — see SidebarResponsive in code-behind) -->
            <Border Grid.Column="0"
                    x:Name="SidebarPanel"
                    Width="{DynamicResource SidebarW}"
                    Background="{DynamicResource SurfaceRailBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0">
                <DockPanel Margin="12,14,12,12">
                <!-- Pinned footer: compact settings + hotkey-help icon buttons -->
                <Border DockPanel.Dock="Bottom"
                        Margin="0,10,0,0"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="0,8,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Left">
                        <Border x:Name="SettingsNavItem"
                                Classes="footer-icon-item"
                                PointerPressed="SettingsNav_OnPointerPressed"
                                ToolTip.Tip="{DynamicResource Settings}"
                                AutomationProperties.AutomationId="SettingsNavItem">
                            <!-- no nav-glyph: that class adds a right margin for sidebar rows and would
                                 push the gear off-centre inside the square footer button -->
                            <PathIcon Data="{StaticResource IconGear}"/>
                        </Border>
                        <Border x:Name="HotkeyHelpButton"
                                Classes="footer-icon-item"
                                PointerPressed="HotkeyHelpButton_OnPointerPressed"
                                ToolTip.Tip="{DynamicResource HotkeyPanelTitle}"
                                AutomationProperties.AutomationId="HotkeyHelpButton">
                            <TextBlock Classes="footer-help-glyph" Text="?"/>
                        </Border>
                    </StackPanel>
                </Border>
                <ScrollViewer Classes="sidebar-scroll"
                              HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              AllowAutoHide="False"
                              Margin="0,0,-8,0">
                <StackPanel Spacing="0" Margin="0,0,4,0">
                    <TextBlock Classes="section-eyebrow"
                               Text="{DynamicResource ViewsGroup}"
                               Margin="8,0,0,10"/>
                    <ListBox x:Name="SidebarNav"
                             Classes="sidebar-nav"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="0"
                             SelectionChanged="SidebarNav_OnSelectionChanged"
                             AutomationProperties.AutomationId="SidebarNav">
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconList}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource AllTasks}"/>
                                <Border Grid.Column="2"
                                        Classes="count-pill neutral"
                                        IsVisible="{Binding CurrentAllTasksItems.Count, Converter={converters:CountToBoolConverter}}"
                                        AutomationProperties.AutomationId="AllTasksCountPill">
                                    <TextBlock Text="{Binding CurrentAllTasksItems.Count}"/>
                                </Border>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconPlus}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource LastCreated}"/>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconPencil}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource LastUpdated}"/>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem Classes="now">
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconBolt}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource Unlocked}"/>
                                <Border Grid.Column="2"
                                        Classes="count-pill"
                                        IsVisible="{Binding UnlockedItems.Count, Converter={converters:CountToBoolConverter}}"
                                        AutomationProperties.AutomationId="ReadyNowCountPill">
                                    <TextBlock Text="{Binding UnlockedItems.Count}"/>
                                </Border>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconClock}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource InProgress}"/>
                                <Border Grid.Column="2"
                                        Classes="count-pill neutral"
                                        IsVisible="{Binding InProgressItems.Count, Converter={converters:CountToBoolConverter}}"
                                        AutomationProperties.AutomationId="InProgressCountPill">
                                    <TextBlock Text="{Binding InProgressItems.Count}"/>
                                </Border>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconCheck}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource Completed}"/>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconArchive}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource Archived}"/>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconEye}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource LastOpened}"/>
                            </Grid>
                        </ListBoxItem>
                        <ListBoxItem>
                            <Grid ColumnDefinitions="Auto,*">
                                <PathIcon Classes="nav-glyph" Data="{StaticResource IconMap}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Text="{DynamicResource Roadmap}"/>
                            </Grid>
                        </ListBoxItem>
                    </ListBox>

                    <!-- GOALS group: top-level tasks (goals) — collapsible header -->
                    <Border Classes="goals-header"
                            x:Name="SidebarGoalsHeader"
                            PointerPressed="GoalsHeader_OnPointerPressed"
                            AutomationProperties.AutomationId="SidebarGoalsHeader"
                            IsVisible="{Binding CurrentAllTasksItems.Count, Converter={converters:CountToBoolConverter}}">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Grid.Column="0" Classes="section-eyebrow"
                                       VerticalAlignment="Center"
                                       Text="{DynamicResource GoalsGroup}"/>
                            <PathIcon Grid.Column="1" Classes="goals-chevron"
                                      x:Name="GoalsChevron"
                                      Data="{StaticResource IconChevronDown}"/>
                        </Grid>
                    </Border>
                    <ItemsControl x:Name="SidebarGoalsList"
                                  ItemsSource="{Binding CurrentAllTasksItems}"
                                  AutomationProperties.AutomationId="SidebarGoals">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="viewModel:TaskWrapperViewModel">
                                <Border Classes="goal-row"
                                        PointerPressed="GoalItem_OnPointerPressed"
                                        DataContext="{Binding TaskItem}">
                                    <Grid ColumnDefinitions="Auto,*,Auto">
                                        <Border Classes="goal-tile" Grid.Column="0">
                                            <PathIcon Classes="goal-glyph" Data="{StaticResource IconTarget}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                            <TextBlock TextTrimming="CharacterEllipsis"
                                                       Text="{Binding Title}"/>
                                            <ProgressBar Classes="goal-progress"
                                                         Minimum="0" Maximum="1"
                                                         IsVisible="{Binding HasChildren}"
                                                         Value="{Binding GoalProgress}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2"
                                                   Classes="meta"
                                                   VerticalAlignment="Center"
                                                   Margin="6,0,0,0"
                                                   IsVisible="{Binding HasChildren}"
                                                   Text="{Binding GoalProgressPercent, StringFormat='{}{0}%'}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                </ScrollViewer>
                </DockPanel>
            </Border>

        <!--Панель навигации-->
        <SplitView x:Name="MainNavigationSplitView"
                   Grid.Column="1"
                   IsPaneOpen="{Binding DetailsAreOpen}"
                   DisplayMode="CompactInline"
                   UseLightDismissOverlayMode="False"
                   OpenPaneLength="{Binding #mainControl.Bounds.Width, Converter={converters:MinValueConverter}, ConverterParameter=600}"
                   CompactPaneLength="20"
                   PanePlacement="Right">
            <Grid x:Name="MainTabsHost"
                  Margin="16,14,12,0"
                  HorizontalAlignment="Stretch">
                <TabControl x:Name="MainTabs"
                            TabStripPlacement="Top"
                            ClipToBounds="True"
                            Margin="0"
                            Padding="0"
                            SelectionChanged="MainTabs_OnSelectionChanged"
                            AutomationProperties.AutomationId="MainTabs">
                    <TabControl.Styles>
                        <Style Selector="TabItem">
                            <Setter Property="FontFamily" Value="{DynamicResource UiFontFamily}"/>
                            <Setter Property="FontSize" Value="{DynamicResource AppTabFontSize}"/>
                            <Setter Property="Padding" Value="0,0,6,0"/>
                            <Setter Property="MinHeight" Value="{DynamicResource AppTabMinHeight}"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                            <Setter Property="Foreground" Value="{DynamicResource Text2Brush}"/>
                        </Style>
                        <Style Selector="TabItem:pointerover /template/ ContentPresenter#PART_ContentPresenter">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        </Style>
                        <Style Selector="TabItem:selected /template/ ContentPresenter#PART_ContentPresenter">
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                        </Style>
                        <Style Selector="TabItem:selected">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                        <Style Selector="TabControl /template/ ContentPresenter#PART_SelectedContentHost">
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                        <!-- Aurora: collapse the horizontal tab strip (views live in the sidebar)
                             but keep it in the tree so tab items stay focusable (hotkeys route
                             through MainControl after a tab switch). -->
                        <Style Selector="TabControl /template/ ItemsPresenter#PART_ItemsPresenter">
                            <Setter Property="Height" Value="0"/>
                            <Setter Property="Opacity" Value="0"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                            <Setter Property="ClipToBounds" Value="True"/>
                        </Style>
                    </TabControl.Styles>
                    <!--All Tasks-->
                    <TabItem Header="{DynamicResource AllTasks}" IsSelected= "{Binding AllTasksMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="AllTasksTabItem">
                        <Grid RowDefinitions= "Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="AllTasksFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="AllTasksFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                        <StackPanel Spacing="10">
                                                            <StackPanel>
                                                                <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterSort}"/>
                                                                <ComboBox ItemsSource="{Binding SortDefinitions}" SelectedItem="{Binding CurrentSortDefinition}" MinWidth="220"/>
                                                            </StackPanel>
                                                            <StackPanel>
                                                                <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                                <WrapPanel>
                                                                    <ComboBox ItemsSource="{Binding StatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="AllTasksStatusFilterComboBox"/>
                                                                </WrapPanel>
                                                            </StackPanel>
                                                            <Button Classes="FilterPanelResetButton"
                                                                    ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                    AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                    Command="{Binding ResetTaskFiltersCommand}"
                                                                    AutomationProperties.AutomationId="AllTasksResetFiltersButton">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                    <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Button>
                                                        </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row= "1" AutoScrollToSelectedItem= "True"
                                      ItemsSource="{Binding CurrentAllTasksItems}"
                                      SelectedItem="{Binding CurrentAllTasksItem}"
                                      SelectionMode= "Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="AllTasksTree"
                                      x:Name="AllTasksTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType= "viewModel:TaskWrapperViewModel" ItemsSource= "{Binding SubTasks}">
                                        <ContentControl Content= "{Binding .}" PointerPressed= "InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased" DoubleTapped= "TaskTree_OnDoubleTapped"/>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Last Created-->
                    <TabItem Header="{DynamicResource LastCreated}" IsSelected="{Binding LastCreatedMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="LastCreatedTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="LastCreatedFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="LastCreatedFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                    <StackPanel Spacing="10">
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterDate}"/>
                                                            <WrapPanel>
                                                                <CheckBox Content="{DynamicResource CustomPeriod}" IsChecked="{Binding LastCreatedDateFilter.IsCustom}" Margin="0,0,12,6" VerticalAlignment="Center"/>
                                                                <ComboBox IsVisible="{Binding !LastCreatedDateFilter.IsCustom}" ItemsSource="{Binding DateFilterDefinitions}" SelectedItem="{Binding LastCreatedDateFilter.CurrentOption}" VerticalAlignment="Center" Margin="0,0,8,6" />
                                                            </WrapPanel>
                                                            <WrapPanel IsVisible="{Binding LastCreatedDateFilter.IsCustom}">
                                                                <Label Content="{DynamicResource From}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding LastCreatedDateFilter.From}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding LastCreatedDateFilter.From, Converter={converters:DayOrTodayValueConverter}}"/>
                                                                <Label Content="{DynamicResource To}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding LastCreatedDateFilter.To}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding LastCreatedDateFilter.To, Converter={converters:DayOrTodayValueConverter}}"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding LastCreatedStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="LastCreatedStatusFilterComboBox"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <Button Classes="FilterPanelResetButton"
                                                                ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                Command="{Binding ResetTaskFiltersCommand}"
                                                                AutomationProperties.AutomationId="LastCreatedResetFiltersButton">
                                                            <StackPanel Orientation="Horizontal">
                                                                <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding LastCreatedItems}"
                                      SelectedItem="{Binding CurrentLastCreated}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="LastCreatedTree"
                                      x:Name="LastCreatedTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.CreatedDateTime, StringFormat={}{0:yyyy.MM.dd}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="2" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="4" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Last Updated-->
                    <TabItem Header="{DynamicResource LastUpdated}" IsSelected="{Binding LastUpdatedMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="LastUpdatedTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="LastUpdatedFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="LastUpdatedFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                    <StackPanel Spacing="10">
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterDate}"/>
                                                            <WrapPanel>
                                                                <CheckBox Content="{DynamicResource CustomPeriod}" IsChecked="{Binding LastUpdatedDateFilter.IsCustom}" Margin="0,0,12,6" VerticalAlignment="Center"/>
                                                                <ComboBox IsVisible="{Binding !LastUpdatedDateFilter.IsCustom}" ItemsSource="{Binding DateFilterDefinitions}" SelectedItem="{Binding LastUpdatedDateFilter.CurrentOption}" VerticalAlignment="Center" Margin="0,0,8,6" />
                                                            </WrapPanel>
                                                            <WrapPanel IsVisible="{Binding LastUpdatedDateFilter.IsCustom}">
                                                                <Label Content="{DynamicResource From}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding LastUpdatedDateFilter.From}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding LastUpdatedDateFilter.From, Converter={converters:DayOrTodayValueConverter}}"/>
                                                                <Label Content="{DynamicResource To}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding LastUpdatedDateFilter.To}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding LastUpdatedDateFilter.To, Converter={converters:DayOrTodayValueConverter}}"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding LastUpdatedStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="LastUpdatedStatusFilterComboBox"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <Button Classes="FilterPanelResetButton"
                                                                ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                Command="{Binding ResetTaskFiltersCommand}"
                                                                AutomationProperties.AutomationId="LastUpdatedResetFiltersButton">
                                                            <StackPanel Orientation="Horizontal">
                                                                <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding LastUpdatedItems}"
                                      SelectedItem="{Binding CurrentLastUpdated}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="LastUpdatedTree"
                                      x:Name="LastUpdatedTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.UpdatedDateTime, StringFormat={}{0:yyyy.MM.dd}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="2" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="4" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Unlocked-->
                    <TabItem Header="{DynamicResource Unlocked}" IsSelected="{Binding UnlockedMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="UnlockedTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="UnlockedFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="UnlockedFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                    <StackPanel Spacing="10">
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterSort}"/>
                                                            <ComboBox ItemsSource="{Binding SortDefinitions}" SelectedItem="{Binding CurrentSortDefinitionForUnlocked}" MinWidth="220"/>
                                                        </StackPanel>
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding UnlockedStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="UnlockedStatusFilterComboBox"/>
                                                                <ComboBox ItemsSource="{Binding WantedFilterDefinitions}" SelectedItem="{Binding CurrentWantedFilter}" MinWidth="160" Margin="0,0,8,6" AutomationProperties.AutomationId="UnlockedWantedFilterComboBox"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterTiming}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding UnlockedTimeFilters}" Margin="0,0,8,6" />
                                                                <ComboBox ItemsSource="{Binding DurationFilters}" Margin="0,0,8,6" />
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <Button Classes="FilterPanelResetButton"
                                                                ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                Command="{Binding ResetTaskFiltersCommand}"
                                                                AutomationProperties.AutomationId="UnlockedResetFiltersButton">
                                                            <StackPanel Orientation="Horizontal">
                                                                <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding UnlockedItems}"
                                      SelectedItem="{Binding CurrentUnlockedItem}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="UnlockedTree"
                                      x:Name="UnlockedTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="2"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="3" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--In Progress-->
                    <TabItem Header="{DynamicResource InProgress}" IsSelected="{Binding InProgressMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="InProgressTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="InProgressFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="InProgressFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                        <StackPanel Spacing="10">
                                                            <StackPanel>
                                                                <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                                <WrapPanel>
                                                                    <ComboBox ItemsSource="{Binding InProgressStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="InProgressStatusFilterComboBox"/>
                                                                </WrapPanel>
                                                            </StackPanel>
                                                            <Button Classes="FilterPanelResetButton"
                                                                    ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                    AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                    Command="{Binding ResetTaskFiltersCommand}"
                                                                    AutomationProperties.AutomationId="InProgressResetFiltersButton">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                    <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                            </Button>
                                                        </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding InProgressItems}"
                                      SelectedItem="{Binding CurrentInProgressItem}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="InProgressTree"
                                      x:Name="InProgressTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1"
                                                       Classes="TaskTreeMetaLabel"
                                                       Content="{Binding TaskItem.StartedLocalDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm}}"
                                                       Foreground="{StaticResource AccentButtonBackground}"
                                                       FontFamily="Consolas"
                                                       VerticalAlignment="Center"
                                                       AutomationProperties.AutomationId="InProgressStartedDateLabel" />
                                                <Label Grid.Column="2" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.InProgressElapsed}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" AutomationProperties.AutomationId="InProgressElapsedLabel" />
                                                <Label Grid.Column="3" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="4"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="5" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Completed-->
                    <TabItem Header="{DynamicResource Completed}" IsSelected="{Binding CompletedMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="CompletedTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="CompletedFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="CompletedFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                    <StackPanel Spacing="10">
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding CompletedStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="CompletedStatusFilterComboBox"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterDate}"/>
                                                            <WrapPanel>
                                                                <CheckBox Content="{DynamicResource CustomPeriod}" IsChecked="{Binding CompletedDateFilter.IsCustom}" Margin="0,0,12,6" VerticalAlignment="Center"/>
                                                                <ComboBox IsVisible="{Binding !CompletedDateFilter.IsCustom}" ItemsSource="{Binding DateFilterDefinitions}" SelectedItem="{Binding CompletedDateFilter.CurrentOption}" VerticalAlignment="Center" Margin="0,0,8,6" />
                                                            </WrapPanel>
                                                            <WrapPanel IsVisible="{Binding CompletedDateFilter.IsCustom}">
                                                                <Label Content="{DynamicResource From}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding CompletedDateFilter.From}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding CompletedDateFilter.From, Converter={converters:DayOrTodayValueConverter}}"/>
                                                                <Label Content="{DynamicResource To}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding CompletedDateFilter.To}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding CompletedDateFilter.To, Converter={converters:DayOrTodayValueConverter}}"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <Button Classes="FilterPanelResetButton"
                                                                ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                Command="{Binding ResetTaskFiltersCommand}"
                                                                AutomationProperties.AutomationId="CompletedResetFiltersButton">
                                                            <StackPanel Orientation="Horizontal">
                                                                <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding CompletedItems}"
                                      SelectedItem="{Binding CurrentCompletedItem}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="CompletedTree"
                                      x:Name="CompletedTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.CompletedDateTime, StringFormat={}{0:yyyy.MM.dd}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.ArchiveDateTime, StringFormat={}{0:yyyy.MM.dd}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="2" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="4" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Archived-->
                    <TabItem Header="{DynamicResource Archived}" IsSelected="{Binding ArchivedMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="ArchivedTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiFilters}"
                                                                           Watermark="{DynamicResource EmojiFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="IncludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="IncludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="IncludeEmojiFilterDropDown"
                                                                           ListAutomationId="IncludeEmojiFilterList"
                                                                           NoMatchesAutomationId="IncludeEmojiFilterNoMatches"/>
                                    <views:EmojiFilterMultiSelectSearchBox Filters="{Binding EmojiExcludeFilters}"
                                                                           Watermark="{DynamicResource EmojiExcludeFilterSearchWatermark}"
                                                                           NoMatchesText="{DynamicResource EmojiFilterNoMatches}"
                                                                           IsExclude="True"
                                                                           SummaryWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryWidth}"
                                                                           SummaryMinWidth="{x:Static views:EmojiFilterMultiSelectSearchBox.DefaultSummaryMinWidth}"
                                                                           SummaryAutomationId="ExcludeEmojiFilterSummaryBox"
                                                                           SearchAutomationId="ExcludeEmojiFilterSearchBox"
                                                                           DropDownAutomationId="ExcludeEmojiFilterDropDown"
                                                                           ListAutomationId="ExcludeEmojiFilterList"
                                                                           NoMatchesAutomationId="ExcludeEmojiFilterNoMatches"/>
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="ArchivedFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="ArchivedFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                    <StackPanel Spacing="10">
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding ArchivedStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="ArchivedStatusFilterComboBox"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterDate}"/>
                                                            <WrapPanel>
                                                                <CheckBox Content="{DynamicResource CustomPeriod}" IsChecked="{Binding ArchivedDateFilter.IsCustom}" Margin="0,0,12,6" VerticalAlignment="Center" />
                                                                <ComboBox IsVisible="{Binding !ArchivedDateFilter.IsCustom}" ItemsSource="{Binding DateFilterDefinitions}" SelectedItem="{Binding ArchivedDateFilter.CurrentOption}" VerticalAlignment="Center" Margin="0,0,8,6" />
                                                            </WrapPanel>
                                                            <WrapPanel IsVisible="{Binding ArchivedDateFilter.IsCustom}">
                                                                <Label Content="{DynamicResource From}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding ArchivedDateFilter.From}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding ArchivedDateFilter.From, Converter={converters:DayOrTodayValueConverter}}"/>
                                                                <Label Content="{DynamicResource To}" Margin="0,0,6,6" VerticalAlignment="Center"/>
                                                                <CalendarDatePicker Margin="0,0,8,6"
                                                                                    SelectedDate="{Binding ArchivedDateFilter.To}"
                                                                                    IsTodayHighlighted="True"
                                                                                    DisplayDate="{Binding ArchivedDateFilter.To, Converter={converters:DayOrTodayValueConverter}}"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <Button Classes="FilterPanelResetButton"
                                                                ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                Command="{Binding ResetTaskFiltersCommand}"
                                                                AutomationProperties.AutomationId="ArchivedResetFiltersButton">
                                                            <StackPanel Orientation="Horizontal">
                                                                <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding ArchivedItems}"
                                      SelectedItem="{Binding CurrentArchivedItem}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="ArchivedTree"
                                      x:Name="ArchivedTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.CompletedDateTime, StringFormat={}{0:yyyy.MM.dd}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding TaskItem.ArchiveDateTime, StringFormat={}{0:yyyy.MM.dd}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="2" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="4" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Last Opened-->
                    <TabItem Header="{DynamicResource LastOpened}" IsSelected="{Binding LastOpenedMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="LastOpenedTabItem">
                        <Grid RowDefinitions="Auto,*">
                            <Grid Classes="FilterToolbar" ColumnDefinitions="Auto,*">
                                <WrapPanel Grid.Column="0" Classes="FilterToolbarItems">
                                    <DropDownButton Classes="FilterToolbarFiltersButton"
                                                    ToolTip.Tip="{DynamicResource Filters}"
                                                    AutomationProperties.Name="{DynamicResource Filters}"
                                                    Margin="0"
                                                    AutomationProperties.AutomationId="LastOpenedFiltersButton">
                                        <PathIcon Classes="FilterToolbarFiltersIcon" Width="15" Height="15" Data="M3,5 L21,5 L21,7 L3,7 Z M7,11 L17,11 L17,13 L7,13 Z M10,17 L14,17 L14,19 L10,19 Z"/>
                                        <DropDownButton.Flyout>
                                            <Flyout Placement="BottomEdgeAlignedLeft">
                                                <Border Classes="FilterOverflowPanel"
                                                        MinWidth="0"
                                                        MaxWidth="360"
                                                        AutomationProperties.AutomationId="LastOpenedFilterPanel">
                                                    <ScrollViewer Classes="FilterPanelScrollViewer">
                                                    <StackPanel Spacing="10">
                                                        <StackPanel>
                                                            <TextBlock Classes="FilterPanelGroupTitle" Text="{DynamicResource FilterState}"/>
                                                            <WrapPanel>
                                                                <ComboBox ItemsSource="{Binding LastOpenedStatusFilters}" MinWidth="220" Margin="0,0,8,6" AutomationProperties.AutomationId="LastOpenedStatusFilterComboBox"/>
                                                            </WrapPanel>
                                                        </StackPanel>
                                                        <Button Classes="FilterPanelResetButton"
                                                                ToolTip.Tip="{DynamicResource ResetFilters}"
                                                                AutomationProperties.Name="{DynamicResource ResetFilters}"
                                                                Command="{Binding ResetTaskFiltersCommand}"
                                                                AutomationProperties.AutomationId="LastOpenedResetFiltersButton">
                                                            <StackPanel Orientation="Horizontal">
                                                                <PathIcon Classes="FilterPanelResetIcon" Width="16" Height="16" Data="M17.65,6.35 C16.2,4.9 14.21,4 12,4 C7.58,4 4,7.58 4,12 C4,16.42 7.58,20 12,20 C15.73,20 18.84,17.45 19.73,14 L17.65,14 C16.82,16.33 14.61,18 12,18 C8.69,18 6,15.31 6,12 C6,8.69 8.69,6 12,6 C13.66,6 15.14,6.69 16.22,7.78 L13,11 L20,11 L20,4 Z"/>
                                                                <TextBlock Text="{DynamicResource ResetFilters}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Button>
                                                    </StackPanel>
                                                    </ScrollViewer>
                                                </Border>
                                            </Flyout>
                                        </DropDownButton.Flyout>
                                    </DropDownButton>
                                </WrapPanel>
                                <searchControl:SearchBar Grid.Column="1" Classes="FilterToolbarSearch" DataContext="{Binding Search}"/>
                            </Grid>
                            <TreeView Grid.Row="1" AutoScrollToSelectedItem="True"
                                      ItemsSource="{Binding LastOpenedItems}"
                                      SelectedItem="{Binding CurrentLastOpenedItem}"
                                      SelectionMode="Multiple"
                                      Classes="TaskTree"
                                      PointerPressed="TaskTree_OnPointerPressed"
                                      KeyDown="TaskTree_OnKeyDown"
                                      AutomationProperties.AutomationId="LastOpenedTree"
                                      x:Name="LastOpenedTree">
                                <TreeView.ItemTemplate>
                                    <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                        <Grid DoubleTapped="TaskTree_OnDoubleTapped" Background="Transparent">
                                            <Grid ColumnDefinitions="Auto, Auto, Auto, Auto,*" DragDrop.AllowDrop="True" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Border Classes="ready-dot" Margin="0,0,6,0" VerticalAlignment="Center" IsVisible="{Binding TaskItem.IsCanBeCompleted}" AutomationProperties.AutomationId="TaskReadyDot"/>
                                                    <unlimotion:TaskStatusPicker Task="{Binding TaskItem}" VerticalAlignment="Center" AutomationProperties.AutomationId="TaskStatusButton" />
                                                </StackPanel>
                                                <Label Grid.Column="1" Classes="TaskTreeMetaLabel" Content="{Binding SpecialDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm:ss}}" Foreground="{StaticResource AccentButtonBackground}" FontFamily="Consolas" VerticalAlignment="Center" />
                                                <Label Grid.Column="2" Content="{Binding TaskItem.GetAllEmoji}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding TaskItem.RepeaterListMarker}"
                                                           IsVisible="{Binding TaskItem.IsHaveRepeater}"
                                                           ToolTip.Tip="{Binding TaskItem.RepeaterListMarkerToolTip}"
                                                           AutomationProperties.AutomationId="TaskRepeaterListMarker"
                                                           Margin="4,0,4,0"
                                                           VerticalAlignment="Center" />
                                                <Grid Grid.Column="4" VerticalAlignment="Center">
                                                    <unlimotion:EmojiTextBlock EmojiText="{Binding TaskItem.Title}" VerticalAlignment="Center" HorizontalAlignment="Left" AutomationProperties.AutomationId="InlineTaskTitleTextBlock" Classes.IsWanted="{Binding TaskItem.Wanted}" Classes.IsCanBeCompleted="{Binding !TaskItem.IsCanBeCompleted}" PointerPressed="InlineTaskTitleText_OnPointerPressed"/>
                                                </Grid>
                                            </Grid>
                                            <Button Classes="TaskRowRemoveButton"
                                                    ToolTip.Tip="{DynamicResource Remove}"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding RemoveCommand}">
                                                <PathIcon Classes="RemoveButtonIcon" Data="M15.5,5.9 L14.1,4.5 L10,8.6 L5.9,4.5 L4.5,5.9 L8.6,10 L4.5,14.1 L5.9,15.5 L10,11.4 L14.1,15.5 L15.5,14.1 L11.4,10 Z"/>
                                            </Button>
                                        </Grid>
                                    </TreeDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Grid>
                    </TabItem>
                    <!--Roadmap-->
                    <TabItem Header="{DynamicResource Roadmap}" IsSelected="{Binding GraphMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="RoadmapTabItem">
                        <views:GraphControl DataContext="{Binding Graph}"/>
                    </TabItem>
                    <!--Settings-->
                    <TabItem Header="{DynamicResource Settings}" IsSelected="{Binding SettingsMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="SettingsTabItem">
                        <views:SettingsControl DataContext="{Binding Settings}"/>
                    </TabItem>
                    <!--Profile (личный кабинет): reached via the avatar in the header, not a visible tab-->
                    <TabItem Header="{DynamicResource ProfileTitle}" IsSelected="{Binding ProfileMode, Mode=OneWayToSource}"
                             AutomationProperties.AutomationId="ProfileTabItem">
                        <views:ProfileControl DataContext="{Binding Profile}"/>
                    </TabItem>
                </TabControl>
                <Button x:Name="MainTabsOverflowButton"
                        Classes="MainTabsOverflowButton"
                        ZIndex="1"
                        HorizontalAlignment="Left"
                        IsVisible="False"
                        AutomationProperties.AutomationId="MainTabsOverflowButton">
                    <Grid Width="18"
                          Height="14"
                          RowDefinitions="2,5,2,5,2"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          AutomationProperties.AutomationId="MainTabsOverflowIcon">
                        <Border Grid.Row="0"
                                Classes="MainTabsOverflowIconLine"
                                Width="18"
                                Height="2"
                                CornerRadius="1"
                                Background="{StaticResource AccentButtonBackground}"
                                AutomationProperties.AutomationId="MainTabsOverflowIconLine1"/>
                        <Border Grid.Row="2"
                                Classes="MainTabsOverflowIconLine"
                                Width="18"
                                Height="2"
                                CornerRadius="1"
                                Background="{StaticResource AccentButtonBackground}"
                                AutomationProperties.AutomationId="MainTabsOverflowIconLine2"/>
                        <Border Grid.Row="4"
                                Classes="MainTabsOverflowIconLine"
                                Width="18"
                                Height="2"
                                CornerRadius="1"
                                Background="{StaticResource AccentButtonBackground}"
                                AutomationProperties.AutomationId="MainTabsOverflowIconLine3"/>
                    </Grid>
                    <Button.Flyout>
                        <MenuFlyout/>
                    </Button.Flyout>
                </Button>
            </Grid>
            <SplitView.Pane>
                <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ColumnDefinitions="*,Auto">
                    <ToggleButton Width="20" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Content="↔️" VerticalAlignment="Stretch" Grid.Column="1" IsChecked="{Binding !DetailsAreOpen}"
                                  AutomationProperties.AutomationId="DetailsPaneToggleButton"></ToggleButton>
                    <Border x:Name="CurrentTaskDetailsPanelFrame"
                            Grid.Column="0"
                            Classes="CurrentTaskDetailsPanelFrame"
                            AutomationProperties.ControlTypeOverride="Group"
                            AutomationProperties.IsControlElementOverride="True"
                            AutomationProperties.AutomationId="CurrentTaskDetailsPanelFrame"
                            IsVisible="{Binding DetailsAreOpen}">
                        <ScrollViewer x:Name="CurrentTaskDetailsScrollViewer"
                                      AutomationProperties.AutomationId="CurrentTaskDetailsScrollViewer"
                                      HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Auto"
                                      VerticalContentAlignment="Top"
                                      behavior:KeyboardAwareScrollViewer.IsEnabled="True">
                            <StackPanel x:Name="TaskDetailsPanelRoot" Classes="TaskDetailsPanel">
                            <Border Classes="CurrentTaskCard"
                                    AutomationProperties.ControlTypeOverride="Group"
                                    AutomationProperties.IsControlElementOverride="True"
                                    AutomationProperties.AutomationId="CurrentTaskCard"
                                    IsVisible="{Binding CurrentTaskItem, Converter={x:Static ObjectConverters.IsNotNull}}">
                                <StackPanel Classes="CurrentTaskCardContent"
                                            DataContext="{Binding CurrentTaskItem}">
                                    <Grid Classes="TaskCardHeader"
                                          ColumnDefinitions="Auto,*"
                                          RowDefinitions="Auto,Auto,Auto"
                                          AutomationProperties.ControlTypeOverride="Group"
                                          AutomationProperties.IsControlElementOverride="True"
                                          AutomationProperties.AutomationId="CurrentTaskHeader">
                                        <unlimotion:TaskStatusPicker Task="{Binding}"
                                                  Margin="0,0,8,0"
                                                  VerticalAlignment="Center"
                                                  ToolTip.Tip="{Binding StatusToolTip}"
                                                  AutomationProperties.AutomationId="CurrentTaskStatusButton"/>
                                        <TextBox Grid.Column="1"
                                                 x:Name="CurrentTaskTitleTextBox"
                                                 AutomationProperties.AutomationId="CurrentTaskTitleTextBox"
                                                 Classes="CurrentTaskTitleEditor BorderlessTextBoxChrome"
                                                 TextWrapping="Wrap"
                                                 Text="{Binding Title}"
                                                 PlaceholderText="{DynamicResource TitleWatermark}"
                                                 VerticalAlignment="Center"
                                                 VerticalContentAlignment="Center"/>

                                        <!-- tags + actions -->
                                        <Grid Grid.Row="1" Grid.ColumnSpan="2"
                                              ColumnDefinitions="*,Auto"
                                              Margin="0,8,0,0">
                                            <WrapPanel Grid.Column="0" Classes="TaskHeaderTagsRow" VerticalAlignment="Center">
                                                <Border Classes="status-tag ready"
                                                        IsVisible="{Binding IsCanBeCompleted}"
                                                        AutomationProperties.AutomationId="CurrentTaskReadyTag">
                                                    <TextBlock Text="{DynamicResource ReadyNow}"/>
                                                </Border>
                                                <Border Classes="status-tag blocked"
                                                        IsVisible="{Binding IsBlocked}"
                                                        AutomationProperties.AutomationId="CurrentTaskBlockedTag">
                                                    <TextBlock Text="{DynamicResource Blocked}"/>
                                                </Border>
                                                <Border Classes="status-tag weekly"
                                                        IsVisible="{Binding IsHaveRepeater}"
                                                        AutomationProperties.AutomationId="CurrentTaskWeeklyTag">
                                                    <TextBlock Text="{DynamicResource RepeatsWeekly}"/>
                                                </Border>
                                                <CheckBox IsChecked="{Binding WantedFromUi}"
                                                          Classes="TaskHeaderStateCheckBox"
                                                          VerticalAlignment="Center"
                                                          Content="{DynamicResource Wanted}"
                                                          AutomationProperties.AutomationId="CurrentTaskWantedCheckBox" />
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="6"
                                                            ToolTip.Tip="{DynamicResource ImportanceHint}">
                                                    <TextBlock Text="{DynamicResource Importance}" Classes="caption" VerticalAlignment="Center"/>
                                                    <NumericUpDown Value="{Binding Importance}"
                                                                   Classes="TaskHeaderImportanceInput"
                                                                   Minimum="0"
                                                                   Maximum="100"
                                                                   ToolTip.Tip="{DynamicResource ImportanceHint}"
                                                                   AutomationProperties.AutomationId="CurrentTaskImportanceInput"/>
                                                </StackPanel>
                                            </WrapPanel>
                                            <Border Grid.Column="1"
                                                    VerticalAlignment="Center"
                                                    AutomationProperties.ControlTypeOverride="Group"
                                                    AutomationProperties.IsControlElementOverride="True"
                                                    AutomationProperties.AutomationId="CurrentTaskCommandBar">
                                            <DropDownButton Classes="TaskActionsMenuButton TaskAccentOutlineButton TaskIconOnlyDropDownButton"
                                                            Content="⋮"
                                                            VerticalAlignment="Center"
                                                            ToolTip.Tip="{DynamicResource TaskActionsMenuTip}"
                                                            AutomationProperties.Name="{DynamicResource TaskActionsMenuTip}"
                                                            AutomationProperties.AutomationId="CurrentTaskActionsMenuButton">
                                                <DropDownButton.Flyout>
                                                    <MenuFlyout>
                                                        <MenuItem Header="{DynamicResource MoveToPath}"
                                                                  Command="{Binding MainWindow.MoveToPath}"
                                                                  AutomationProperties.AutomationId="CurrentTaskMoveToPathMenuItem">
                                                            <MenuItem.Icon>
                                                                <TextBlock Text="📁"/>
                                                            </MenuItem.Icon>
                                                        </MenuItem>
                                                        <MenuItem Header="{DynamicResource Archive}"
                                                                  Command="{Binding ArchiveCommand}"
                                                                  AutomationProperties.AutomationId="CurrentTaskArchiveMenuItem">
                                                            <MenuItem.Icon>
                                                                <TextBlock Text="📦"/>
                                                            </MenuItem.Icon>
                                                        </MenuItem>
                                                        <MenuItem Header="{DynamicResource Remove}"
                                                                  Command="{Binding MainWindow.Remove}"
                                                                  AutomationProperties.AutomationId="CurrentTaskRemoveMenuItem">
                                                            <MenuItem.Icon>
                                                                <TextBlock Text="🗑️"/>
                                                            </MenuItem.Icon>
                                                        </MenuItem>
                                                    </MenuFlyout>
                                                </DropDownButton.Flyout>
                                            </DropDownButton>
                                            </Border>
                                        </Grid>

                                        <!-- metadata: id + timestamps, each with its own label -->
                                        <WrapPanel Grid.Row="2" Grid.ColumnSpan="2" Classes="TaskHeaderMetaLine" Margin="0,8,0,0">
                                            <StackPanel Orientation="Horizontal" Spacing="4" MaxWidth="180">
                                                <TextBlock Classes="meta TaskHeaderMetaLabel" Text="ID"/>
                                                <TextBlock Classes="meta TaskHeaderIdMeta"
                                                           MaxWidth="140"
                                                           TextTrimming="CharacterEllipsis"
                                                           Text="{Binding Id, Mode=OneWay}"
                                                           ToolTip.Tip="{Binding Id}"
                                                           AutomationProperties.AutomationId="CurrentTaskIdTextBlock"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="4"
                                                        IsVisible="{Binding CreatedDateTime, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                <TextBlock Classes="meta TaskHeaderMetaLabel" Text="{DynamicResource CreatedPrefix}"/>
                                                <TextBlock Classes="meta" Text="{Binding CreatedDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm}}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="4"
                                                        IsVisible="{Binding UpdatedDateTime, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                <TextBlock Classes="meta TaskHeaderMetaLabel" Text="{DynamicResource UpdatedPrefix}"/>
                                                <TextBlock Classes="meta" Text="{Binding UpdatedDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm}}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="4"
                                                        IsVisible="{Binding UnlockedDateTime, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                <TextBlock Classes="meta TaskHeaderMetaLabel" Text="{DynamicResource UnlockedPrefix}"/>
                                                <TextBlock Classes="meta" Text="{Binding UnlockedDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm}}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="4"
                                                        IsVisible="{Binding CompletedDateTime, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                <TextBlock Classes="meta TaskHeaderMetaLabel" Text="{DynamicResource CompletedPrefix}"/>
                                                <TextBlock Classes="meta" Text="{Binding CompletedDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm}}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Spacing="4"
                                                        IsVisible="{Binding ArchiveDateTime, Converter={x:Static ObjectConverters.IsNotNull}}">
                                                <TextBlock Classes="meta TaskHeaderMetaLabel" Text="{DynamicResource ArchivedPrefix}"/>
                                                <TextBlock Classes="meta" Text="{Binding ArchiveDateTime, StringFormat={}{0:yyyy.MM.dd HH:mm}}"/>
                                            </StackPanel>
                                        </WrapPanel>
                                    </Grid>

                                    <Border Classes="TaskCardSection"
                                            AutomationProperties.ControlTypeOverride="Group"
                                            AutomationProperties.IsControlElementOverride="True"
                                            AutomationProperties.AutomationId="CurrentTaskDescriptionSection">
                                        <StackPanel Classes="TaskCardSectionContent">
                                            <TextBox Text="{Binding Description}"
                                                     AutomationProperties.AutomationId="CurrentTaskDescriptionTextBox"
                                                     Classes="TaskDescriptionEditor"
                                                     TextWrapping="Wrap"
                                                     AcceptsReturn="True"
                                                     AcceptsTab="True"
                                                     HorizontalAlignment="Stretch"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Classes="TaskCardSection"
                                            AutomationProperties.ControlTypeOverride="Group"
                                            AutomationProperties.IsControlElementOverride="True"
                                            AutomationProperties.AutomationId="CurrentTaskCompletionCriteriaSection">
                                        <StackPanel Classes="TaskCardSectionContent">
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Classes="TaskCardSectionTitle"
                                                           Text="{DynamicResource CompletionCriteria}"
                                                           VerticalAlignment="Center"/>
                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource Add}"
                                                        Command="{Binding AddCompletionCriterionCommand}"
                                                        IsEnabled="{Binding CanEditCompletionCriteria}"
                                                        AutomationProperties.AutomationId="AddCompletionCriterionButton"/>
                                            </Grid>
                                            <ItemsControl ItemsSource="{Binding CompletionCriteria}"
                                                          IsEnabled="{Binding CanEditCompletionCriteria}"
                                                          AutomationProperties.AutomationId="CompletionCriteriaItems"/>
                                        </StackPanel>
                                    </Border>

                                    <Border Classes="TaskCardSection"
                                            AutomationProperties.ControlTypeOverride="Group"
                                            AutomationProperties.IsControlElementOverride="True"
                                            AutomationProperties.AutomationId="CurrentTaskPlanningSection">
                                        <UniformGrid x:Name="PlanningFieldsGrid" Columns="3">
                                                    <Grid Classes="TaskPlanningFieldRow" ColumnDefinitions="*,Auto">
                                                        <CalendarDatePicker SelectedDate="{Binding PlannedBeginDateTime}"
                                                                            Classes="TaskPlanningValueControl"
                                                                            AutomationProperties.AutomationId="CurrentTaskPlannedBeginPicker"
                                                                            IsTodayHighlighted="True"
                                                                            PlaceholderText="{DynamicResource PlannedBegin}"
                                                                            UseFloatingPlaceholder="True"
                                                                            HorizontalAlignment="Stretch"
                                                                            DisplayDate="{Binding PlannedBeginDateTime, Mode=OneWay, Converter={converters:DayOrTodayValueConverter}}"/>
                                                        <DropDownButton Grid.Column="1"
                                                                        Content="📅"
                                                                        ToolTip.Tip="{DynamicResource SetBegin}"
                                                                        AutomationProperties.Name="{DynamicResource SetBegin}"
                                                                        Classes="TaskPlanningQuickAction TaskAccentOutlineButton TaskIconOnlyDropDownButton"
                                                                        AutomationProperties.AutomationId="CurrentTaskSetBeginButton">
                                                            <DropDownButton.Flyout>
                                                                <MenuFlyout>
                                                                    <MenuItem Header="{DynamicResource Today}" Command="{Binding Commands.SetBeginToday}"/>
                                                                    <MenuItem Header="{DynamicResource Tomorrow}" Command="{Binding Commands.SetBeginTomorrow}"/>
                                                                    <MenuItem Header="{DynamicResource NextMonday}" Command="{Binding Commands.SetBeginNextMonday}"/>
                                                                    <MenuItem Header="{DynamicResource FirstDayNextMonth}" Command="{Binding Commands.SetBegin1DayNextMonth}"/>
                                                                    <MenuItem Header="{DynamicResource None}" Command="{Binding Commands.SetBeginNone}"/>
                                                                </MenuFlyout>
                                                            </DropDownButton.Flyout>
                                                        </DropDownButton>
                                                    </Grid>
                                                    <Grid Classes="TaskPlanningFieldRow" ColumnDefinitions="*,Auto">
                                                        <TextBox ToolTip.Tip="{DynamicResource DurationExample}"
                                                                 Classes="TaskPlanningValueControl"
                                                                 AutomationProperties.AutomationId="CurrentTaskPlannedDurationTextBox"
                                                                 HorizontalAlignment="Stretch"
                                                                 behavior:LostFocusUpdateBindingBehavior.Text="{Binding PlannedDuration, Converter={StaticResource TimeSpanStringConverter}}"
                                                                 PlaceholderText="{DynamicResource PlannedDuration}"
                                                                 UseFloatingPlaceholder="True"/>
                                                        <DropDownButton Grid.Column="1"
                                                                        Content="⏱"
                                                                        ToolTip.Tip="{DynamicResource SetDuration}"
                                                                        AutomationProperties.Name="{DynamicResource SetDuration}"
                                                                        Classes="TaskPlanningQuickAction TaskAccentOutlineButton TaskIconOnlyDropDownButton"
                                                                        AutomationProperties.AutomationId="CurrentTaskSetDurationButton">
                                                            <DropDownButton.Flyout>
                                                                <MenuFlyout>
                                                                    <MenuItem Header="{DynamicResource OneMinute}" Command="{Binding SetDurationCommands.OneMinCommand}" />
                                                                    <MenuItem Header="{DynamicResource FiveMinutes}" Command="{Binding SetDurationCommands.FiveMinutesCommand}" />
                                                                    <MenuItem Header="{DynamicResource TenMinutes}" Command="{Binding SetDurationCommands.TenMinutesCommand}" />
                                                                    <MenuItem Header="{DynamicResource TwentyMinutes}" Command="{Binding SetDurationCommands.TwentyMinutesCommand}" />
                                                                    <MenuItem Header="{DynamicResource FortyMinutes}" Command="{Binding SetDurationCommands.FortyMinutesCommand}" />
                                                                    <MenuItem Header="{DynamicResource OneHour}" Command="{Binding SetDurationCommands.OneHourCommand}" />
                                                                    <MenuItem Header="{DynamicResource TwoHours}" Command="{Binding SetDurationCommands.TwoHoursCommand}" />
                                                                    <MenuItem Header="{DynamicResource FourHours}" Command="{Binding SetDurationCommands.FourHoursCommand}" />
                                                                    <MenuItem Header="{DynamicResource OneDay}" Command="{Binding SetDurationCommands.OneDayCommand}" />
                                                                    <MenuItem Header="{DynamicResource TwoDays}" Command="{Binding SetDurationCommands.TwoDaysCommand}" />
                                                                    <MenuItem Header="{DynamicResource FourDays}" Command="{Binding SetDurationCommands.FourDaysCommand}" />
                                                                    <MenuItem Header="{DynamicResource EightDays}" Command="{Binding SetDurationCommands.EightDaysCommand}" />
                                                                    <MenuItem Header="{DynamicResource None}" Command="{Binding SetDurationCommands.NoneCommand}" />
                                                                </MenuFlyout>
                                                            </DropDownButton.Flyout>
                                                        </DropDownButton>
                                                    </Grid>
                                                    <Grid Classes="TaskPlanningFieldRow" ColumnDefinitions="*,Auto">
                                                        <CalendarDatePicker SelectedDate="{Binding PlannedEndDateTime}"
                                                                            Classes="TaskPlanningValueControl"
                                                                            AutomationProperties.AutomationId="CurrentTaskPlannedEndPicker"
                                                                            IsTodayHighlighted="True"
                                                                            PlaceholderText="{DynamicResource PlannedEnd}"
                                                                            UseFloatingPlaceholder="True"
                                                                            HorizontalAlignment="Stretch"
                                                                            DisplayDate="{Binding PlannedEndDateTime, Mode=OneWay, Converter={converters:DayOrTodayValueConverter}}"/>
                                                        <DropDownButton Grid.Column="1"
                                                                        Content="🏁"
                                                                        ToolTip.Tip="{DynamicResource SetEnd}"
                                                                        AutomationProperties.Name="{DynamicResource SetEnd}"
                                                                        Classes="TaskPlanningQuickAction TaskAccentOutlineButton TaskIconOnlyDropDownButton"
                                                                        AutomationProperties.AutomationId="CurrentTaskSetEndButton">
                                                            <DropDownButton.Flyout>
                                                                <MenuFlyout>
                                                                    <MenuItem Header="{DynamicResource Today}" Command="{Binding Commands.SetEndToday}"/>
                                                                    <MenuItem Header="{DynamicResource Tomorrow}" Command="{Binding Commands.SetEndTomorrow}"/>
                                                                    <MenuItem Header="{DynamicResource NextFriday}" Command="{Binding Commands.SetEndNextFriday}"/>
                                                                    <MenuItem Header="{DynamicResource OneDay}" Command="{Binding Commands.SetEnd1Days}"/>
                                                                    <MenuItem Header="{DynamicResource FiveDays}" Command="{Binding Commands.SetEnd5Days}"/>
                                                                    <MenuItem Header="{DynamicResource SevenDays}" Command="{Binding Commands.SetEnd7Days}"/>
                                                                    <MenuItem Header="{DynamicResource TenDays}" Command="{Binding Commands.SetEnd10Days}"/>
                                                                    <MenuItem Header="{DynamicResource OneMonth}" Command="{Binding Commands.SetEnd1Month}"/>
                                                                    <MenuItem Header="{DynamicResource None}" Command="{Binding Commands.SetEndNone}"/>
                                                                </MenuFlyout>
                                                            </DropDownButton.Flyout>
                                                        </DropDownButton>
                                                    </Grid>
                                        </UniformGrid>
                                    </Border>

                                    <Border Classes="TaskCardSection"
                                            AutomationProperties.ControlTypeOverride="Group"
                                            AutomationProperties.IsControlElementOverride="True"
                                            AutomationProperties.AutomationId="CurrentTaskRepeaterSection">
                                        <StackPanel Classes="TaskCardSectionContent">
                                            <Grid Classes="RepeaterControls RepeaterPatternControls"
                                                  ColumnDefinitions="*,*,Auto,*"
                                                  RowDefinitions="Auto,Auto">
                                                <ComboBox Grid.Column="0"
                                                          Classes="RepeaterSelector"
                                                          ItemsSource="{Binding Repeaters}"
                                                          SelectedItem="{Binding Repeater}"
                                                          PlaceholderText="{DynamicResource RepeaterTemplates}"
                                                          AutomationProperties.AutomationId="CurrentTaskRepeaterSelector">
                                                    <ComboBox.ItemTemplate>
                                                        <DataTemplate DataType="viewModel:RepeaterPatternViewModel">
                                                            <Label Content="{Binding Title}"/>
                                                        </DataTemplate>
                                                    </ComboBox.ItemTemplate>
                                                </ComboBox>
                                                <ComboBox Grid.Column="1"
                                                          Classes="RepeaterPatternTypeSelector"
                                                          ItemsSource="{Binding Repeater.RepeaterTypes}"
                                                          SelectedItem="{Binding Repeater.SelectedRepeaterType}"
                                                          IsVisible="{Binding IsHaveRepeater}"
                                                          AutomationProperties.AutomationId="CurrentTaskRepeaterPatternTypeSelector"/>
                                                <NumericUpDown Grid.Column="2"
                                                               Classes="RepeaterPeriodInput"
                                                               Value="{Binding Repeater.Period}"
                                                               IsVisible="{Binding IsHaveRepeater}"
                                                               PlaceholderText="{DynamicResource Period}"
                                                               ToolTip.Tip="{DynamicResource Period}"
                                                               AutomationProperties.AutomationId="CurrentTaskRepeaterPeriodInput"/>
                                                <CheckBox Grid.Column="3"
                                                          Classes="RepeaterAfterCompleteCheckBox"
                                                          IsChecked="{Binding Repeater.AfterComplete}"
                                                          IsVisible="{Binding IsHaveRepeater}"
                                                          Content="{DynamicResource AfterComplete}"
                                                          AutomationProperties.AutomationId="CurrentTaskRepeaterAfterCompleteCheckBox"/>
                                                <WrapPanel Grid.Row="1"
                                                           Grid.Column="1"
                                                           Grid.ColumnSpan="3"
                                                           Classes="WeekdayToggles"
                                                           IsVisible="{Binding Repeater.Type, Converter={StaticResource EqualConverter}, ConverterParameter={x:Static domain:RepeaterType.Weekly}, FallbackValue={x:Static domain:RepeaterType.None}, TargetNullValue={x:Static domain:RepeaterType.None}}">
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Monday}" Content="{DynamicResource MondayShort}" ToolTip.Tip="{DynamicResource Monday}" />
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Tuesday}" Content="{DynamicResource TuesdayShort}" ToolTip.Tip="{DynamicResource Tuesday}" />
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Wednesday}" Content="{DynamicResource WednesdayShort}" ToolTip.Tip="{DynamicResource Wednesday}"/>
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Thursday}" Content="{DynamicResource ThursdayShort}" ToolTip.Tip="{DynamicResource Thursday}" />
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Friday}" Content="{DynamicResource FridayShort}" ToolTip.Tip="{DynamicResource Friday}" />
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Saturday}" Content="{DynamicResource SaturdayShort}" ToolTip.Tip="{DynamicResource Saturday}" />
                                                    <ToggleButton Classes="WeekdayToggle" IsChecked="{Binding Repeater.Sunday}" Content="{DynamicResource SundayShort}" ToolTip.Tip="{DynamicResource Sunday}" />
                                                </WrapPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Border>

                                    <Border Classes="TaskCardSection"
                                            AutomationProperties.ControlTypeOverride="Group"
                                            AutomationProperties.IsControlElementOverride="True"
                                            AutomationProperties.AutomationId="CurrentTaskRelationsSection">
                                        <StackPanel Classes="TaskCardSectionContent">
                                    <Grid Classes="RelationSectionHeader" ColumnDefinitions="*,Auto">
                                        <Label Content="{DynamicResource ParentsTasks}"/>
                                        <Button Grid.Column="1"
                                                Classes="RelationAddButton TaskAccentOutlineButton"
                                                Content="＋"
                                                ToolTip.Tip="{DynamicResource AddParentRelation}"
                                                AutomationProperties.Name="{DynamicResource AddParentRelation}"
                                                Click="RelationAddButton_OnClick"
                                                Tag="Parents"
                                                AutomationProperties.AutomationId="CurrentTaskParentsRelationAddButton"/>
                                    </Grid>
                                    <Border Classes="RelationEditorCard"
                                            DataContext="{Binding MainWindow.CurrentRelationEditor}"
                                            IsVisible="{Binding IsOpenForParents}">
                                        <StackPanel Classes="RelationPickerRow">
                                            <TextBlock Text="{Binding Header}" FontWeight="Bold"/>
                                            <TextBox Classes="RelationEditorSearch"
                                                     Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     PlaceholderText="{Binding Watermark}"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskParentsRelationAddInput"/>
                                            <ListBox Classes="RelationEditorSuggestions"
                                                     ItemsSource="{Binding Suggestions}"
                                                     SelectedItem="{Binding SelectedCandidate, Mode=TwoWay}"
                                                     DoubleTapped="RelationEditorSuggestions_OnDoubleTapped"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskParentsRelationSuggestions"/>
                                            <TextBlock Text="{Binding EmptyStateText}"
                                                       IsVisible="{Binding !HasSuggestions}"
                                                       Opacity="0.7"
                                                       TextWrapping="Wrap"/>
                                            <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="8" HorizontalAlignment="Right">
                                                <Button Content="{DynamicResource Cancel}"
                                                        Command="{Binding CancelCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskParentsRelationAddCancelButton"/>
                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource Add}"
                                                        Command="{Binding ConfirmCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskParentsRelationAddConfirmButton"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    <TreeView DataContext="{Binding MainWindow}"
                                              ItemsSource="{Binding CurrentItemParents.SubTasks}"
                                              SelectionMode="Multiple"
                                              Classes="TaskTree RelationTaskTree"
                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                              PointerPressed="TaskTree_OnPointerPressed"
                                              AutomationProperties.AutomationId="CurrentItemParentsTree"
                                              x:Name="CurrentItemParentsTree">
                                        <TreeView.ItemTemplate>
                                            <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                                <ContentControl Content="{Binding .}" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased" DoubleTapped="Task_OnDoubleTapped"/>
                                            </TreeDataTemplate>
                                        </TreeView.ItemTemplate>
                                    </TreeView>

                                    <Grid Classes="RelationSectionHeader" ColumnDefinitions="*,Auto">
                                        <Label Content="{DynamicResource BlockingTasks}"/>
                                        <Button Grid.Column="1"
                                                Classes="RelationAddButton TaskAccentOutlineButton"
                                                Content="＋"
                                                ToolTip.Tip="{DynamicResource AddBlockingRelation}"
                                                AutomationProperties.Name="{DynamicResource AddBlockingRelation}"
                                                Click="RelationAddButton_OnClick"
                                                Tag="Blocking"
                                                AutomationProperties.AutomationId="CurrentTaskBlockingRelationAddButton"/>
                                    </Grid>
                                    <Border Classes="RelationEditorCard"
                                            DataContext="{Binding MainWindow.CurrentRelationEditor}"
                                            IsVisible="{Binding IsOpenForBlocking}">
                                        <StackPanel Classes="RelationPickerRow">
                                            <TextBlock Text="{Binding Header}" FontWeight="Bold"/>
                                            <TextBox Classes="RelationEditorSearch"
                                                     Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     PlaceholderText="{Binding Watermark}"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskBlockingRelationAddInput"/>
                                            <ListBox Classes="RelationEditorSuggestions"
                                                     ItemsSource="{Binding Suggestions}"
                                                     SelectedItem="{Binding SelectedCandidate, Mode=TwoWay}"
                                                     DoubleTapped="RelationEditorSuggestions_OnDoubleTapped"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskBlockingRelationSuggestions"/>
                                            <TextBlock Text="{Binding EmptyStateText}"
                                                       IsVisible="{Binding !HasSuggestions}"
                                                       Opacity="0.7"
                                                       TextWrapping="Wrap"/>
                                            <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="8" HorizontalAlignment="Right">
                                                <Button Content="{DynamicResource Cancel}"
                                                        Command="{Binding CancelCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskBlockingRelationAddCancelButton"/>
                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource Add}"
                                                        Command="{Binding ConfirmCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskBlockingRelationAddConfirmButton"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    <TreeView DataContext="{Binding MainWindow}"
                                              ItemsSource="{Binding CurrentItemBlockedBy.SubTasks}"
                                              SelectionMode="Multiple"
                                              Classes="TaskTree RelationTaskTree"
                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                              PointerPressed="TaskTree_OnPointerPressed"
                                              AutomationProperties.AutomationId="CurrentItemBlockedByTree"
                                              x:Name="CurrentItemBlockedByTree">
                                        <TreeView.ItemTemplate>
                                            <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                                <ContentControl Content="{Binding .}" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased" DoubleTapped="Task_OnDoubleTapped"/>
                                            </TreeDataTemplate>
                                        </TreeView.ItemTemplate>
                                    </TreeView>

                                    <Grid Classes="RelationSectionHeader" ColumnDefinitions="*,Auto">
                                        <Label Content="{DynamicResource ContainingTasks}"/>
                                        <Button Grid.Column="1"
                                                Classes="RelationAddButton TaskAccentOutlineButton"
                                                Content="＋"
                                                ToolTip.Tip="{DynamicResource AddContainingRelation}"
                                                AutomationProperties.Name="{DynamicResource AddContainingRelation}"
                                                Click="RelationAddButton_OnClick"
                                                Tag="Containing"
                                                AutomationProperties.AutomationId="CurrentTaskContainingRelationAddButton"/>
                                    </Grid>
                                    <Border Classes="RelationEditorCard"
                                            DataContext="{Binding MainWindow.CurrentRelationEditor}"
                                            IsVisible="{Binding IsOpenForContaining}">
                                        <StackPanel Classes="RelationPickerRow">
                                            <TextBlock Text="{Binding Header}" FontWeight="Bold"/>
                                            <TextBox Classes="RelationEditorSearch"
                                                     Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     PlaceholderText="{Binding Watermark}"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskContainingRelationAddInput"/>
                                            <ListBox Classes="RelationEditorSuggestions"
                                                     ItemsSource="{Binding Suggestions}"
                                                     SelectedItem="{Binding SelectedCandidate, Mode=TwoWay}"
                                                     DoubleTapped="RelationEditorSuggestions_OnDoubleTapped"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskContainingRelationSuggestions"/>
                                            <TextBlock Text="{Binding EmptyStateText}"
                                                       IsVisible="{Binding !HasSuggestions}"
                                                       Opacity="0.7"
                                                       TextWrapping="Wrap"/>
                                            <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="8" HorizontalAlignment="Right">
                                                <Button Content="{DynamicResource Cancel}"
                                                        Command="{Binding CancelCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskContainingRelationAddCancelButton"/>
                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource Add}"
                                                        Command="{Binding ConfirmCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskContainingRelationAddConfirmButton"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    <TreeView DataContext="{Binding MainWindow}"
                                              ItemsSource="{Binding CurrentItemContains.SubTasks}"
                                              SelectionMode="Multiple"
                                              Classes="TaskTree RelationTaskTree"
                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                              PointerPressed="TaskTree_OnPointerPressed"
                                              AutomationProperties.AutomationId="CurrentItemContainsTree"
                                              x:Name="CurrentItemContainsTree">
                                        <TreeView.ItemTemplate>
                                            <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                                <ContentControl Content="{Binding .}" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased" DoubleTapped="Task_OnDoubleTapped"/>
                                            </TreeDataTemplate>
                                        </TreeView.ItemTemplate>
                                    </TreeView>

                                    <Grid Classes="RelationSectionHeader" ColumnDefinitions="*,Auto">
                                        <Label Content="{DynamicResource BlockedTasks}"/>
                                        <Button Grid.Column="1"
                                                Classes="RelationAddButton TaskAccentOutlineButton"
                                                Content="＋"
                                                ToolTip.Tip="{DynamicResource AddBlockedRelation}"
                                                AutomationProperties.Name="{DynamicResource AddBlockedRelation}"
                                                Click="RelationAddButton_OnClick"
                                                Tag="Blocked"
                                                AutomationProperties.AutomationId="CurrentTaskBlockedRelationAddButton"/>
                                    </Grid>
                                    <Border Classes="RelationEditorCard"
                                            DataContext="{Binding MainWindow.CurrentRelationEditor}"
                                            IsVisible="{Binding IsOpenForBlocked}">
                                        <StackPanel Classes="RelationPickerRow">
                                            <TextBlock Text="{Binding Header}" FontWeight="Bold"/>
                                            <TextBox Classes="RelationEditorSearch"
                                                     Text="{Binding Query, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     PlaceholderText="{Binding Watermark}"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskBlockedRelationAddInput"/>
                                            <ListBox Classes="RelationEditorSuggestions"
                                                     ItemsSource="{Binding Suggestions}"
                                                     SelectedItem="{Binding SelectedCandidate, Mode=TwoWay}"
                                                     DoubleTapped="RelationEditorSuggestions_OnDoubleTapped"
                                                     KeyDown="RelationEditorControl_OnKeyDown"
                                                     AutomationProperties.AutomationId="CurrentTaskBlockedRelationSuggestions"/>
                                            <TextBlock Text="{Binding EmptyStateText}"
                                                       IsVisible="{Binding !HasSuggestions}"
                                                       Opacity="0.7"
                                                       TextWrapping="Wrap"/>
                                            <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="8" HorizontalAlignment="Right">
                                                <Button Content="{DynamicResource Cancel}"
                                                        Command="{Binding CancelCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskBlockedRelationAddCancelButton"/>
                                                <Button Grid.Column="1"
                                                        Content="{DynamicResource Add}"
                                                        Command="{Binding ConfirmCommand}"
                                                        AutomationProperties.AutomationId="CurrentTaskBlockedRelationAddConfirmButton"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                    <TreeView DataContext="{Binding MainWindow}"
                                              ItemsSource="{Binding CurrentItemBlocks.SubTasks}"
                                              SelectionMode="Multiple"
                                              Classes="TaskTree RelationTaskTree"
                                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                              PointerPressed="TaskTree_OnPointerPressed"
                                              AutomationProperties.AutomationId="CurrentItemBlocksTree"
                                              x:Name="CurrentItemBlocksTree">
                                        <TreeView.ItemTemplate>
                                            <TreeDataTemplate DataType="viewModel:TaskWrapperViewModel" ItemsSource="{Binding SubTasks}">
                                                <ContentControl Content="{Binding .}" PointerPressed="InputElement_OnPointerPressed" PointerMoved="InputElement_OnPointerMoved" PointerReleased="InputElement_OnPointerReleased" DoubleTapped="Task_OnDoubleTapped"/>
                                            </TreeDataTemplate>
                                        </TreeView.ItemTemplate>
                                    </TreeView>

                                        </StackPanel>
                                    </Border>

                                    <Border Classes="TaskCardSection"
                                            AutomationProperties.ControlTypeOverride="Group"
                                            AutomationProperties.IsControlElementOverride="True"
                                            AutomationProperties.AutomationId="CurrentTaskStatusHistorySection">
                                        <Expander Header="{DynamicResource StatusHistory}"
                                                  Classes="TaskStatusHistoryExpander"
                                                  ExpandDirection="Down"
                                                  AutomationProperties.AutomationId="StatusHistoryExpander">
                                            <ItemsControl ItemsSource="{Binding StatusHistory}"
                                                          AutomationProperties.AutomationId="StatusHistoryItems"/>
                                        </Expander>
                                    </Border>

                                </StackPanel>
                            </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </SplitView.Pane>
        </SplitView>
        <!--Редактор задачи-->
        </Grid>
        <!-- Hotkey help overlay: spans the whole root grid (RowSpan=2) above all content -->
        <Grid x:Name="HotkeyHelpOverlayHost"
              Grid.RowSpan="2"
              Panel.ZIndex="100"
              IsVisible="False"
              Focusable="True"
              Background="#66000000"
              AutomationProperties.AutomationId="HotkeyHelpOverlayHost">
            <Border HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MaxWidth="640"
                    MaxHeight="600"
                    Margin="24"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource BorderStrongBrush}"
                    CornerRadius="{DynamicResource RadiusMd}"
                    Background="{DynamicResource SurfaceRaisedBrush}"
                    AutomationProperties.AutomationId="HotkeyHelpOverlayPanelFrame">
                <views:HotkeyHelpPanel x:Name="EmbeddedHotkeyHelpPanel"/>
            </Border>
        </Grid>
    </Grid>

</UserControl>
